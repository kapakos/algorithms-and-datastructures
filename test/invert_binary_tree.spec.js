const InvertBinaryTree = require('../lib/invert_binary_tree');
const TreeNode = require('../lib/tree_node');
describe('InvertBinaryTree', () => {
  const tree = new TreeNode(4, 
    new TreeNode(2, 
      new TreeNode(1),
      new TreeNode(3)),
    new TreeNode(7,
      new TreeNode(6),
      new TreeNode(9)));

  it('build the right tree', () => {
     
  }); 
})
